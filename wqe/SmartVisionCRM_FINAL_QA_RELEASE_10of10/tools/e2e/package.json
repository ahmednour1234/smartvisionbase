{
  "name": "sv-crm-e2e",
  "private": true,
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "typescript": "^5.5.0"
  },
  "scripts": {
    "test": "playwright test",
    "install:browsers": "playwright install --with-deps"
  }
}
